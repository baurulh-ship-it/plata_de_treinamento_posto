DATABASE_URL="file:./training.db"
# Gere uma chave aleatória com pelo menos 32 caracteres. npm run setup faz isso automaticamente.
JWT_SECRET=""
PORT=3000
# Em produção, use HTTPS e NODE_ENV=production (cookie Secure).
